extends pug/layout.pug

block var
  //TODO: Цифры ничего не решают, создай отдельный класс на отступ
  - const mainClass = 'stack gap-40 gap-md-50 gap-xl-100';
  - const pageCurrent = pages.index.href;

block meta
  title Главная

block content
  +main-slider
  +section-content({title: pages.actions.text})
  +blog-place
  +section-content({title: "Новинки"})
  +wedding-offer
  +section-content({title: "Успей купить"})
  +collection
